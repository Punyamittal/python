"""Question: Write a recursive function that calculates the factorial of a number, but include an if-else condition to return "Negative Input" if the number is negative."""
n=int(input())
def fact(n):
    if n<0:
        print("number is negative")
    else:
        if n==0:
            return 1
        else:
            return n*fact(n-1)
print(fact(n))
